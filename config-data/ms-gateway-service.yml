server:
  port: 8080
spring:
  application:
    name: gateway-microservice
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
      routes:
        - id: bff-bank
          url: http://bff-bank:9090
          predicates: 
            - Path=/client/**
